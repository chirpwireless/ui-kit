import { Theme } from '@mui/material';
export declare const getAlertStyles: (theme: Theme, isAlert?: boolean) => {
    backgroundColor: string;
    svg: {
        path: {
            stroke: string;
        };
    };
};
export declare const getHoverStyles: (theme: Theme, isAlert?: boolean) => {
    backgroundColor: string;
};
