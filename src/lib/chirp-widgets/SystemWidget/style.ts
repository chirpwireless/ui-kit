import { Box, styled } from '@mui/material';

// import { Card as BaseCard } from '@/components/Card';

// TODO: take card component from chirp
export const Card = styled(Box)(() => ({
    height: 'auto',
    minHeight: '84px',
}));
