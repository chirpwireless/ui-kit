import React from 'react';
import { IconGallery, IconItem, Meta } from '@storybook/blocks';
import * as icons from './index';

<Meta title="UI/Icons" />

<h2 style={{ marginBottom: 24 }}>Icons</h2>

<p style={{ marginBottom: 24, opacity: 0.7 }}>
    Public icon set. Import from <code>@chirpwireless/ui-kit/icons</code>.
</p>

<IconGallery>
    {Object.entries(icons).map(([key, Icon]) => (
        <IconItem key={key} name={key}>
            <Icon width={24} height={24} />
        </IconItem>
    ))}
</IconGallery>
